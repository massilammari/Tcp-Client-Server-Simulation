# Tcp-Client-Server-Simulation
A Python simulation of a TCP server offering multiple services (FILE, CALC, DB) to multiple concurrent clients. Demonstrates how a real server handles client requests, manages a bounded queue, and drops overflow requests — with full stats tracking.
